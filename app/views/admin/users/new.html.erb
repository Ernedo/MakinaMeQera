<h2>Create User</h2>

<%= simple_form_for(@user, url: admin_create_user_path(@user)) do |f| %>
  <%= f.input :email, label: 'Your username please', error: 'Username is mandatory, please specify one' %>
  <%= f.input :password, hint: 'No special characters.' %>
  <%= f.button :submit %>
<% end %>
